import HeaderEditor from './HeaderEditor'

export default {
    title: 'Components/HeaderEditor',
    component: HeaderEditor,
}

const Template = () => <HeaderEditor></HeaderEditor>;

export const Default = Template.bind({});